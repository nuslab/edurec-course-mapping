[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "edurec-mappings"
version = "0.1.0"
description = "Read-only extraction of NUS EduRec course mapping approval requests to YAML"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "beautifulsoup4>=4.12,<5",
    "Jinja2>=3.1,<4",
    "PyYAML>=6,<7",
    "playwright>=1.40,<2",
    "pydantic>=2.7,<3",
    "pypdf[crypto]>=4,<7",
]

[project.optional-dependencies]
dev = ["ruff>=0.6", "mypy>=1.10", "pytest>=8", "types-PyYAML"]

[project.scripts]
edurec-mappings = "edurec_mappings.cli:main"

[tool.setuptools.packages.find]
where = ["src"]

[tool.setuptools.package-data]
edurec_mappings = ["terms.yaml", "page.js", "templates/*"]

[tool.ruff]
line-length = 100
target-version = "py310"
src = ["src", "tests"]

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B", "SIM", "N", "RUF", "PL", "C4", "ANN"]
ignore = ["PLR0913", "PLR2004"]

[tool.ruff.lint.per-file-ignores]
"tests/*" = ["PLR0913", "PLR0915", "PLC1901"]

[tool.mypy]
python_version = "3.10"
strict = true
files = ["src", "tests"]
warn_unreachable = true

[tool.pytest.ini_options]
testpaths = ["tests"]
